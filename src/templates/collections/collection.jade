extends ../index.jade
block content
  each post in posts
    .post-preview
      a(href=post.url)
        h2.post-title= post.title
        // !=post.contents.toString()
      p.post-meta= moment(post.date).format("LL")
    hr

  ul.pager
    if prev
      li.prev
        a(href=prev.url) <- Newer Posts

    if next
      li.next
        a(href=next.url) Older Posts →

     //  Page #{start} of #{total}
