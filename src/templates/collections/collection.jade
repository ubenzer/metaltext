extends ../index.jade
block content
  div.container
    each post in posts
      div.row
        div.col-sm-12
          h2= post.title
          small= post.date
          !=post.contents.toString()

    =collections
    if prev
      a(href=prev.url) Prev
    if next
      a(href=next.url) Next

    Page #{start} of #{total}